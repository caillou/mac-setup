# Research notes and captured values

Companion to [prd.md](./prd.md). Everything here was read from the personal Mac ("this Mac", user `caillou`, MacBookPro18,2, macOS 26.6.2 build 25G83, Apple Silicon) on 2026-09-10, or verified on the new managed Mac ("new Mac", user `pierre.spring`, macOS 26). Values are the source of truth for the defaults module; commands show how to re-read them.

## 1. New Mac facts

- MDM: Intune. `profiles status -type enrollment` prints `Enrolled via DEP: Yes`, `MDM enrollment: Yes (User Approved)`, server `i.manage.microsoft.com`. A ManageEngine Endpoint Central self-service agent is also installed for app distribution; ignore it for detection.
- `pierre.spring` is in the local admin group (`dscl . -read /Groups/admin GroupMembership`). Verify with `sudo -v` before bootstrapping.
- Xcode Command Line Tools: `xcode-select --install` opened the dialog and downloaded normally, so Intune does not block it.
- The Downloads view hack (section 9) was applied there and verified: Finder shows Downloads in list view with a Date Added column, sorted by it.
- No desktop widgets present. Dock is Apple's default set. Wallpaper appears black. (On this Mac, verified by screenshot on 2026-09-11: wallpaper black, desktop files visible in a column top-right with small icons and labels on the right.) Click-wallpaper-to-show-desktop was active and annoying; `defaults write com.apple.WindowManager EnableStandardClickToShowDesktop -bool false` fixes it immediately.

## 2. This Mac inventory

### Shell

- Login shell `/opt/homebrew/bin/fish`, fish 4.8.1 from Homebrew. Claude Code tool shell is zsh 5.9 via `~/.zprofile`.
- Fisher plugins (old `fishfile` format, must become `fish_plugins`): `jethrokuan/z`, `pure-fish/pure`, `jethrokuan/fzf`. Pure 4.18.0.
- Managed fish files: `config.fish`, `conf.d/fish_frozen_theme.fish` (Ayu Dark colours, generated by fish 4.3 from universal vars), `conf.d/fish_frozen_key_bindings.fish` (only erases a legacy universal var; can be dropped), `functions/karabinerRestart.fish` (restarts Karabiner daemons after wake; keep), `completions/` (generated, do not manage). Other conf.d and functions files belong to plugins.
- `config.fish` today: brew shellenv; `LC_ALL`/`LC_CTYPE`/`LANG` en_US.UTF-8; `LESS`/`MORE` `-R`; PATH prepends `./node_modules/.bin`, `~/.bin` (does not exist); Docker bin; brew completion paths; sources `~/.docker/init-fish.sh`; abbreviations (a atom, ag `rg -S`, c `cal -3`, d date, e code, g lazygit, gb `git branch --sort=-committerdate`, gc `git checkout`, gd `git diff --color-words`, ggl `git pull`, ggp `git push`, glog pretty graph log, gr `git reset HEAD -- .`, gs `git status`, j z, more less, t tmux, unifi java jar, w `wt switch`, wr `wt remove`, watt `system_profiler SPPowerDataType`); `source ~/.asdf/asdf.fish` (old install); pipx path `~/.local/bin`.
- `~/.zprofile` mirrors the non-interactive parts (brew shellenv, locale, XDG_CONFIG_HOME, PATH with node_modules/.bin and ~/.local/bin, asdf.sh, docker init, `wt config shell init zsh`). `~/.zshrc` sources it for non-login shells. `~/.bashrc` contains a stray `echo 'HAHAHAHAHA'`; drop. `~/.inputrc` maps option-arrows to word movement.
- Universal variables (`fish_variables`, not managed) triage:
  - Drop: `BROWSERSTACK_KEY`, `BROWSERSTACK_USERNAME`, `PYENV_ROOT`, `SPACEFISH_CHAR_SYMBOL`, `__fish_init_*`, `_fisher_upgraded_to_4_4`, `fish_user_paths` (empty), 28 `fish_color_*`/`fish_pager_*` vars (superseded by the frozen theme file), `fish_greeting` (default text).
  - Plugin defaults, regenerate themselves: all `FZF_*` (`FZF_TMUX_HEIGHT 40%`, `FZF_DEFAULT_OPTS --height 40%`, `FZF_LEGACY_KEYBINDINGS 1`, `FZF_DISABLE_KEYBINDINGS 0`, `FZF_PREVIEW_DIR_CMD ls`, `FZF_PREVIEW_FILE_CMD head -n 10`), all `Z_*`/`ZO_CMD`.
  - Keep in a conf.d file: `XDG_CONFIG_HOME=$HOME/.config` (exported), `fish_emoji_width 2`, and the `pure_*` options that differ from Pure's defaults (73 are set; diff against `_pure_set_default` calls in Pure's `conf.d/_pure_init.fish` at migration; known non-defaults candidates: `pure_check_for_new_release false`, `pure_begin_prompt_with_current_directory true`, `pure_reverse_prompt_symbol_in_vimode true`).

### Homebrew

- Taps: `gbevin/tools`, `jesseduffield/lazygit`. Trusted formula `gbevin/tools/sendmidi`.
- Leaves (formulae): allure azure-cli bat bat-extras brightness cocoapods dfu-util exiftool fatsort fd ffmpeg figlet fish fontforge fzf gbevin/tools/sendmidi gh git git-gui glow gnutls go gobject-introspection graphicsmagick htop hyfetch imagemagick jq lazygit lefthook libdvdcss libraw lilypond lolcat lua@5.4 luarocks mactop make mas mkcert mtr nethack nss open-ocd openfortivpn pipx platformio python@3.10 python@3.12 qrencode rename ripgrep shared-mime-info shellcheck slides smartmontools sox stress-ng stylua tree unbound uv viu wget worktrunk xpdf yt-dlp. Not present but needed: asdf (Homebrew), chezmoi, dockutil.
- Casks: alfred babeledit balenaetcher discord docker docker-desktop firefox font-ibm-plex-mono font-ibm-plex-sans gcc-arm-embedded geekbench google-chrome google-chrome-canary google-chrome@canary hammerspoon handbrake handbrake-app homerow istat-menus iterm2 karabiner-elements microsoft-edge microsoft-teams nextcloud ngrok obs obsidian signal slack spotify steam telegram the-unarchiver threema vcv-rack visual-studio-code vivaldi vlc. Duplicates to drop: `docker` vs `docker-desktop`, `google-chrome-canary` vs `google-chrome@canary`, `handbrake` vs `handbrake-app`.
- Fonts: `font-ibm-plex-mono`, `font-ibm-plex-sans` casks exist; there is no umbrella `font-ibm-plex`; `font-blex-mono-nerd-font` exists. `~/Library/Fonts` holds BlexMonoNerdFont-*.ttf; `~/repos/nerdfont-patcher` exists, so those may be self-patched. Decide at Brewfile sort whether the cask replaces them.
- App Store (`mas list`): Amphetamine 937984704, Azure VPN Client 1553936137, Bitwarden 1352778147, Blackmagic Disk Speed Test 425264550, Day One 1055511498, Draw Things 6444050820, Firewatch 1164603847, GarageBand 682658836, GIPHY CAPTURE 668208984, Harvest 506189836, iMazing Converter 1292198261, iMovie 408981434, Ivory 6444602274, Keynote 409183694, LG Screen Manager 1142051783, LuminoCity 950290785, Microsoft Outlook 985367838, Monodraw 920404675, Numbers 409203825, Pages 409201541, Stray 6451498949, Tailscale 1475387142, WhatsApp 310633997, Windows App 1295203466, Xcode 497799835.
- Apps in /Applications not from Homebrew or App Store (informational): Bartender 4, Claude, Insomnia, Jump Desktop, Shortcat, UHK Agent, UTM, VirtualBox, Zen Browser, zoom.us, DiffusionBee, GOG Galaxy and others.
- Homebrew facts: bundle merged into brew core 4.5.0 (Apr 2025), supports `mas`, `vscode`, `--global`, `cleanup` asks before removing; 6.0 (Jun 2026) parallel installs. Homebrew installer installs the Command Line Tools headlessly via `softwareupdate` when missing; with a TTY it falls back to the GUI dialog. `NONINTERACTIVE=1` skips the confirm prompt; sudo prompt is unavoidable on a fresh Mac unless `sudo -v` ran first.

### Git, gh, ssh

- `~/.gitconfig`: user Pierre Spring / pierre.spring@caillou.ch; `includeIf gitdir:~/repos/bkw/` and `~/repos/apprentice/` (drop both); `pull.ff = only`; `init.defaultBranch = main`; `core.commentChar = |`. `~/.config/git/ignore`: `**/.claude/settings.local.json`.
- ICFM identity: `pierre.spring@icfm.ch`, used by the AVD session; this Mac commits to ICFM repos with caillou.ch today. New rule: `includeIf gitdir:~/repos/icfm/` → ICFM identity file in the repo.
- gh: logged in as `caillou`, protocol ssh in hosts, alias `co = pr checkout`. `gh auth status` exits non-zero when logged out.
- `~/.ssh/config`: `Host *` with `ForwardAgent yes`, `ServerAliveInterval 60`, `IdentityFile ~/.ssh/id_rsa`. Keys are per machine; Azure DevOps accepts RSA only and has no API for adding keys; GitHub keys can be added with `gh ssh-key add`.

### asdf

- Installed as git clone `~/.asdf` at v0.14.1, sourced via `asdf.fish`/`asdf.sh`. Plugins nodejs and python. `~/.tool-versions`: `python 3.12.13` (node global comes from `~/.nvmrc` = legacy file; current node 24.15.0). `~/.asdfrc`: `legacy_version_file = yes`. Installed node versions 16 to 24. asdf 0.16+ (Go) keeps `~/.asdf` as data dir, so installs survive switching to Homebrew asdf; shell init becomes a PATH entry for `~/.asdf/shims` plus completions. Commands: `asdf set -u` replaces `asdf global`.

### Keyboard projects

- `~/repos/caillou/karabiner.ts` (github caillou/karabiner.ts): `npm run build` runs `tsx src/index.ts`, which calls `writeToProfile('Default profile', ...)` and writes `~/.config/karabiner/karabiner.json`. Done on this Mac on 2026-09-11: the only profile was renamed from `caillou` to `Default profile` by editing the JSON (Karabiner reloaded it live; backup in `~/.config/karabiner/automatic_backups/`), the source was retargeted and committed, and a rebuild produced identical rules. `writeToProfile` fails if the named profile is missing; Karabiner creates `Default profile` on first launch on a fresh Mac.
- `~/repos/keyboard` (github caillou/keyboard, to move to `~/repos/caillou/keyboard`): `make install` runs `script/setup` which installs its Brewfile (hammerspoon cask, lefthook, lua, lua@5.4, luarocks, shellcheck, stylua), symlinks `hammerspoon/` to `~/.hammerspoon/keyboard`, writes `require('keyboard')` into `~/.hammerspoon/init.lua`, installs Lua test deps, relaunches Hammerspoon. Idempotent. `~/.hammerspoon/Spoons/EmmyLua.spoon` is runtime-generated; never manage `Spoons`.
- Karabiner ignores changes when `karabiner.json` is a symlink; the generated JSON stays out of the repo. Hammerspoon config path can be overridden with `defaults write org.hammerspoon.Hammerspoon MJConfigFile` but is not needed.

### iTerm2

- Prefs in `~/Library/Preferences/com.googlecode.iterm2.plist`; not yet in a custom folder (`PrefsCustomFolder`, `LoadPrefsFromCustomFolder` unset). Profile font `IBMPlexMono-Italic 16`. Old `ayu dark.itermcolors` in this repo. Keys to set: `PrefsCustomFolder` (repo folder), `LoadPrefsFromCustomFolder true`, `NoSyncNeverRemindPrefsChangesLostForFile_selection 2` (save automatically).

## 3. Captured macOS settings

Re-read with `defaults read <domain> <key>`; `-g` is NSGlobalDomain.

### Keyboard and text (NSGlobalDomain)

| key | value |
|---|---|
| com.apple.keyboard.fnState | 1 (F-keys are standard) |
| com.apple.HIToolbox AppleFnUsageType | 0 (Fn alone does nothing) |
| KeyRepeat | 2 |
| InitialKeyRepeat | 15 |
| ApplePressAndHoldEnabled | 0 |
| AppleKeyboardUIMode | 3 |
| NSAutomaticCapitalizationEnabled, NSAutomaticDashSubstitutionEnabled, NSAutomaticPeriodSubstitutionEnabled, NSAutomaticQuoteSubstitutionEnabled | 0 (from old script; confirmed quotes = 0) |
| AppleShowAllExtensions | 1 |
| AppleShowScrollBars | Automatic |
| AppleInterfaceStyle | Dark |
| com.apple.swipescrolldirection | 0 (natural scrolling off) |
| AppleEnableSwipeNavigateWithScrolls | 0 |
| com.apple.trackpad.forceClick | 0 |
| com.apple.mouse.tapBehavior | 1 (also `-currentHost`) |
| com.apple.trackpad.enableSecondaryClick (`-currentHost`) | 1 |
| com.apple.trackpad.scrollBehavior (`-currentHost`) | 2 |
| com.apple.trackpad.scaling | 1 in defaults, but live HID value is 45056 = 0.6875; target value 0.6875 |
| com.apple.mouse.scaling, com.apple.mouse.doubleClickThreshold | absent (defaults); live HIDMouseAcceleration 45056; double-click at system default, do not set |

Input source: ABC keyboard layout only. Locale en_CH, languages en-CH fr-CH de-CH it-CH, centimetres (not written by the setup).

### Trackpad (`com.apple.AppleMultitouchTrackpad`; same keys in `com.apple.driver.AppleBluetoothMultitouch.trackpad`)

Clicking 1; DragLock 0; Dragging 0; TrackpadThreeFingerDrag 0; TrackpadRightClick 1; TrackpadCornerSecondaryClick 0; TrackpadThreeFingerTapGesture 0; TrackpadTwoFingerDoubleTapGesture 1; TrackpadPinch 1; TrackpadRotate 1; TrackpadScroll 1; TrackpadHorizScroll 1; TrackpadMomentumScroll 1; TrackpadHandResting 1; TrackpadThreeFingerHorizSwipeGesture 2; TrackpadThreeFingerVertSwipeGesture 2; TrackpadFourFingerHorizSwipeGesture 2; TrackpadFourFingerVertSwipeGesture 2; TrackpadFourFingerPinchGesture 2; TrackpadFiveFingerPinchGesture 2; TrackpadTwoFingerFromRightEdgeSwipeGesture 3; USBMouseStopsTrackpad 0. Built-in only: ForceSuppressed 1; ActuateDetents 0 (silent clicking); FirstClickThreshold 1; SecondClickThreshold 1.

Mouse (`com.apple.driver.AppleBluetoothMultitouch.mouse`): MouseButtonMode OneButton; MouseHorizontalScroll 1; MouseMomentumScroll 1; MouseVerticalScroll 1; MouseTwoFingerDoubleTapGesture 3; MouseTwoFingerHorizSwipeGesture 2; MouseOneFingerDoubleTapGesture 0. Informational.

### Screenshots (`com.apple.screencapture`)

location `~/Downloads`; type png; show-thumbnail 0. Then `killall SystemUIServer` (harmless; value is read at capture time).

### Finder (`com.apple.finder`)

NewWindowTarget PfLo; NewWindowTargetPath `file:///Users/<user>/Downloads/`; FXPreferredViewStyle clmv (column view is the global default; Downloads overrides to list via .DS_Store); ShowStatusBar 1; _FXSortFoldersFirst 1; FXEnableExtensionChangeWarning 0; ShowHardDrivesOnDesktop 0; ShowExternalHardDrivesOnDesktop 1; ShowRemovableMediaOnDesktop 1; ShowMountedServersOnDesktop unset. `com.apple.desktopservices` DSDontWriteNetworkStores 1, DSDontWriteUSBStores 1 (from old script). Global: NSNavPanelExpandedStateForSaveMode(2) 1, PMPrintingExpandedStateForPrint(2) 1, NSDocumentSaveNewDocumentsToCloud 0 (old script). `FK_StandardViewSettings` and `StandardViewSettings` hold identical `ExtendedListViewSettingsV2` dicts (global list-view column defaults, sorted by name); `FK_DefaultListViewSettingsV2` is Finder's seed for Downloads (sorted by dateAdded).

Desktop icons (`DesktopViewSettings.IconViewSettings`): arrangeBy kind; iconSize 16; gridSpacing 100; textSize 14; labelOnBottom 0; showItemInfo 0; showIconPreview 1; backgroundType 0.

### Window manager (`com.apple.WindowManager`)

EnableStandardClickToShowDesktop 0; HideDesktop 1 (= hide items *in Stage Manager*, per nix-darwin's WindowManager catalogue); StandardHideDesktopIcons unset here (= desktop items shown; a 2026-09-11 screenshot of this Mac confirms files are visible top-right). Desired on all machines: StandardHideDesktopIcons 1 (items hidden; applied on this Mac on 2026-09-11 with `defaults write com.apple.WindowManager StandardHideDesktopIcons -bool true && killall Finder`, verified by screenshot: icons gone), HideDesktop 1, StandardHideWidgets 1 (widgets hidden), StageManagerHideWidgets 0; AutoHide 0; EnableTiledWindowMargins 0; EnableTilingOptionAccelerator 0; AppWindowGroupingBehavior 1. Source: https://raw.githubusercontent.com/nix-darwin/nix-darwin/master/modules/system/defaults/WindowManager.nix

### Dock (`com.apple.dock`)

autohide 1; autohide-delay 0.5; autohide-time-modifier unset; tilesize 16; magnification 1; largesize 128; orientation bottom; show-recents 0; launchanim 0; mineffect unset; wvous-tl/tr/bl/br-corner 0 (and -modifier 0). Persistent apps: Freeform (`/System/Applications/Freeform.app`), iPhone Mirroring (`/System/Applications/iPhone Mirroring.app`), Numbers (`/Applications/Numbers.app`). Persistent others: `~/Downloads` with arrangement 2 (date added), displayas 0 (stack), showas 1 (fan). dockutil equivalent: `--section others --view fan --display stack --sort dateadded`. `killall Dock` after changes.

### Wallpaper

Wallpaper store (`~/Library/Application Support/com.apple.wallpaper/Store/Index.plist`) has provider `com.apple.wallpaper.choice.color` with configuration `{type: systemColor, systemColor: {black: {}}}`. Scriptable equivalent: `osascript -e 'tell application "System Events" to set picture of every desktop to "/System/Library/Desktop Pictures/Solid Colors/Black.png"'` (file exists on 26.6). Note: `get picture of every desktop` returns `missing value` when a system colour is set, so read-back is not possible; write unconditionally.

### Dictation

`com.apple.HIToolbox AppleDictationAutoEnable 1`. Shortcut: `com.apple.symbolichotkeys AppleSymbolicHotKeys` entry `164` = `{enabled 1, value {parameters (262144, 18446744073709289471), type modifier}}` (press Control twice). `com.apple.speech.recognition.AppleSpeechRecognition.prefs DictationIMPreferredLanguageIdentifiers` = (en_US, fr_CH, de_CH); `DictationIMUseOnlyOfflineDictation 0`. `com.apple.assistant.support "Dictation Enabled" 1`; offline models installed for en-US, fr-CH, de-CH (`"Offline Dictation Status"` → `Installed 1`). First activation shows a consent dialog; models download via System Settings.

### Power (`pmset -g custom`)

AC: sleep 0; displaysleep 10; womp 1; powernap 1. Battery: sleep 1; displaysleep 15; lessbright 0; womp 0. Both: hibernatemode 3; standby 1; ttyskeepawake 1; tcpkeepalive 1; disksleep 0. Blog post reference (keep AVD awake): the "Prevent automatic sleeping on power adapter when the display is off" switch = `sudo pmset -c sleep 0`.

### Display (not readable without sudo)

Read on 2026-09-11 with `sudo plutil -p /var/root/Library/Preferences/com.apple.CoreBrightness.plist` (root only; no user-level `com.apple.CoreBrightness` or `com.apple.iokit.AmbientLightSensor` domain exists on 26.6):

- Auto-brightness: `DisplayPreferences` → one dict per display UUID → `AutoBrightnessEnable => false` on this Mac (two of four display entries carry the key; the others are external displays with only `BrightnessLevelNits`). Scriptable: with sudo, for every entry under `DisplayPreferences` set `AutoBrightnessEnable` to false (PlistBuddy or plistlib as root), then `sudo killall corebrightnessd`, then read back. The built-in panel's UUID differs per machine, so iterate rather than hardcode.
- True Tone: no key anywhere in that file (searched for Tone, Adapt, Harmony, Enable; Apple's internal name is Harmony and it appears only in analytics timestamps). Likely never toggled here, i.e. at its default. Check the switch in System Settings → Displays on this Mac to decide the target value. Without a known key, True Tone is UI-only: Hammerspoon accessibility scripting of the Displays pane, or manual.
- Also present, informational: Night Shift (`CBUser-*` → `CBBlueReductionStatus`, `BlueReductionEnabled 0`, schedule off), keyboard backlight auto (`KeyboardBacklightABEnabled true`, idle dim time 0).

### Apply without logout

`/System/Library/PrivateFrameworks/SystemAdministration.framework/Resources/activateSettings -u` exists on 26.6.2 and re-applies NSGlobalDomain input settings within seconds when run as the logged-in user (not root). Verified live on this Mac for trackpad scaling, mouse scaling and double-click threshold. Then `killall Finder Dock SystemUIServer`. Quit System Settings first (`osascript -e 'tell application "System Settings" to quit'`).

## 4. Downloads folder view (Date Added list view)

Finder stores a folder's own view settings in the **parent** folder's `.DS_Store` under a record named after the folder. For `~/Downloads` that is `~/.DS_Store`, key `Downloads`. Captured record entries:

- `vstl` type `type` value `Nlsv` (always open in list view)
- `vSrn` type `long` value `1`
- `lsvC` type `blob`: binary plist `{viewOptionsVersion 1, showIconPreview true, calculateAllSizes false, scrollPositionX -156, scrollPositionY 0, textSize 13, iconSize 16, useRelativeDates true, sortColumn dateAdded, columns [...]}` with columns (identifier, visible, ascending, width): name true false 539; ubiquity false false 35; dateModified false false 181; dateCreated true true 181; size true false 97; kind true true 115; dateAdded true false 181; label false true 100; version false true 75; comments false true 300; dateLastOpened false false 192; shareOwner false false 200; shareLastEditor false false 200; invitationStatus false false 210.
- Also present, not needed: `bwsp` (window geometry), `lsvp` (older list-view plist sorted by name), `Iloc`.

Base64 of the exact `lsvC` blob from this Mac:

```

```

Verified write procedure (worked on the new Mac, macOS 26):

```sh
defaults write com.apple.finder NewWindowTarget -string "PfLo"
defaults write com.apple.finder NewWindowTargetPath -string "file://${HOME}/Downloads/"
killall Finder                      # closes all windows so Finder's cache can't overwrite
python3 -m venv /tmp/dsvenv && /tmp/dsvenv/bin/pip -q install ds_store
/tmp/dsvenv/bin/python3 - <<'EOF'
import os, base64, plistlib
from ds_store import DSStore
B64 = "<blob above>"
path = os.path.expanduser('~/.DS_Store')
mode = 'r+' if os.path.exists(path) else 'w+'
with DSStore.open(path, mode) as d:
    d['Downloads']['vstl'] = ('type', b'Nlsv')
    d['Downloads']['vSrn'] = ('long', 1)
    d['Downloads']['lsvC'] = ('blob', base64.b64decode(B64))
EOF
killall Finder
```

Read-back for tests: iterate `DSStore.open(path, 'r')`, filter `e.filename == 'Downloads'`, decode `lsvC` with `plistlib.loads(bytes(e.value))['sortColumn'] == 'dateAdded'`. The `ds_store` library: https://github.com/dmgbuild/ds_store. On this Mac `uv run --with ds_store python3` also works.

Finder's AppleScript dictionary on 26.6 has no Date Added column (`elsv` enum: name, modification date, creation date, size, kind, label, version, comment), so `osascript` cannot do this. `FXPreferredViewStyle` only affects folders without their own record.

### Is there a cleaner way than editing `.DS_Store`? (researched 2026-09-10)

No supported one. Apple's position (DTS) is that `.DS_Store` is Finder-owned and only Finder scripting is supported, and Finder's dictionary has never had a Date Added column. Verdicts:

- Finder AppleScript `sort column of list view options`: works for the eight legacy columns only, not Date Added (enum `elsv` on 26.6.2; same reported for 10.11 to 10.13). https://www.macscripter.net/t/finder-sort-by-date-added/69315
- System Events UI scripting of the View Options window (Cmd+J, `AXSystemFloatingWindow`, checkbox "Date Added", "Sort By" popup): worked on 10.11 in a published script, untested on 26, needs Accessibility and Automation consent, brittle.
- Hammerspoon via `hs.axuielement`/`hs.eventtap`: same route, no published example exists; a live probe on this Mac could not find the View Options window in the accessibility tree, so unverified.
- `defaults` templates (`FK_DefaultListViewSettingsV2`, `FK_StandardViewSettings`): apply only to folders without their own record; Downloads always has one. https://discussions.apple.com/thread/256107879
- Deleting the Downloads record plus `defaults delete com.apple.finder DownloadsFolderListViewSettingsVersion` to make Finder re-seed Downloads sorted by Date Added: plausible, undocumented, one-shot, and loses the home folder's window state. Not chosen.
- Third-party writers (`ds_store`, `finderconf`, `DSStore`, `riddick`): none has a Date Added feature; `finderconf` writes only `lsvP`/`vstl`, which Tahoe ignores for this. Closest precedent: https://github.com/Jingyuan-Zheng/finder-folder-sizes patches `lsvC` in place and restarts Finder, tested on 26.5.
- Copying a whole `.DS_Store` from another Mac: works but coarse, and Finder rewrites lazily.
- MDM profiles and xattrs: cannot express per-folder view settings. https://raw.githubusercontent.com/apple/device-management/release/mdm/profiles/com.apple.finder.yaml

Key format fact: Finder 26 keeps two list-view records per folder. `lsvC` has `columns` as an ordered array with `dateAdded` and is authoritative; `lsvp` is a legacy dict-keyed mirror without `dateAdded`. Write `lsvC`. It is undocumented in every `.DS_Store` format reference and the `ds_store` library has no codec for it, so encode the binary plist yourself.

Improvement adopted for the writer: instead of overwriting with the captured blob, read the existing `lsvC` if present, merge (`sortColumn = dateAdded`, `dateAdded` visible and descending, add the column if missing), and write nothing when already correct. Fall back to the captured blob when no record exists. Optionally also set `FK_DefaultListViewSettingsV2:sortColumn` via PlistBuddy so folders without a record match.

Side effect of the probe on this Mac: a "Terminal wants to control Finder" Automation prompt may be pending, and a Finder window may have been opened. Nothing under `~` was modified.

### Karabiner config generators (researched 2026-09-11)

- **karabiner.ts is maintained and still the best fit.** npm 1.38.0 published 2026-06-13, 1.37.0 (2026-05) added Karabiner-Elements 15.x and 16 features; 0 open issues; listed on Karabiner's own external-generators page. Covers every primitive this config uses (`ifApp`, variables, `to_if_alone`, layers) plus KE 16 additions. Caution: single maintainer, releases months apart, CI publishing switched off in June 2026, so a breaking KE change may wait for a contributor PR. https://github.com/evan-liu/karabiner.ts , https://karabiner-elements.pqrs.org/docs/json/external-json-generators/
- Alternatives: GokuRakuJoudo (Clojure/EDN) slowing, last release 2025-02, behind KE 15.6+/16, 72 open issues. mxstbr/karabiner is a hand-rolled TS generator, not a library. karaml (Python/YAML) stale since 2024. Ruby/Perl DSLs dead. nix-darwin only installs Karabiner, no rules module. Karabiner-Elements 16.x itself (16.3.0, 2026-09-06) can evaluate JavaScript to JSON via `karabiner_cli --eval-js-to-json`, but only for rule snippets, no types or layers, and no official tool writes a profile. kanata (Rust, very active) replaces Karabiner rather than configuring it: runs as root, tied to a DriverKit protocol version, and has no built-in frontmost-app condition, so the Remote Desktop layer would need a separately maintained agent. Rejected.
- **The starter-repo setup is still the documented way** (README "Using Node.js", `tsx src/index.ts`); `npx create-karabiner-config@latest` now scaffolds the same plus a `dev` script `tsx watch src/index.ts`. Deno is officially supported; Bun works but is undocumented.
- Modernisation adopted: pin `"karabiner.ts": "^1.38.0"` and commit the lockfile (with `latest`, a chezmoi change-trigger hash is meaningless); add `"dev": "tsx watch src/index.ts"`; prettier bump optional.
- `writeToProfile()` still fails when the named profile is missing (`Profile <name> not found`) and never creates one. Writing to `'Default profile'` works because Karabiner creates that profile on first launch (ab-dauletkhan/dotfiles does exactly this). `karabiner_cli --select-profile 'Default profile'` selects it; the CLI lives at `/Library/Application Support/org.pqrs/Karabiner-Elements/bin/karabiner_cli`.
- chezmoi repos embedding karabiner.ts: ryo246912/dotfiles (project dir under `dot_config/karabiner-ts`, mise tasks, Bun, pinned), ab-dauletkhan/dotfiles (Deno, pinned 1.36.0, Default profile), MuXiu1997/dotfiles (Deno, generates a JSON that chezmoi applies as a file; manual step). Gai-H/dotfiles uses the `run_onchange_` hash trigger pattern but with jq, not karabiner.ts.

## 5. Research conclusions (with sources)

### Tooling

- chezmoi: copies files; `run_once_`/`run_onchange_` scripts run in alphabetical order, `before_`/`after_` attributes, need a shebang, must be idempotent; templates via Go text/template with `.chezmoi.homeDir`, `.chezmoi.hostname`; `.chezmoi.toml.tmpl` can prompt (`promptBoolOnce`) and set `sourceDir`; `chezmoi re-add` refuses templates; `chezmoi merge` for conflicts. https://www.chezmoi.io/user-guide/setup/ , https://www.chezmoi.io/user-guide/use-scripts-to-perform-actions/ , https://www.chezmoi.io/user-guide/machines/macos/ , https://www.chezmoi.io/user-guide/manage-machine-to-machine-differences/
- Bootstrap one-liner: `sh -c "$(curl -fsLS get.chezmoi.io)" -- init --apply --use-builtin-git=on --source ~/repos/caillou/dotfiles caillou`. `--use-builtin-git=on` is required on a fresh Mac: `/usr/bin/git` is a CLT stub that `exec.LookPath` finds, so chezmoi's `auto` never falls back; the stub opens the CLT dialog and fails. Built-in git is HTTPS-only (go-git). https://www.chezmoi.io/install/ , https://www.chezmoi.io/reference/command-line-flags/global/ , https://github.com/twpayne/chezmoi/issues/1892
- Community practice: pattern A (chezmoi first) with numbered `run_once_before_0x-install-xcode-*` / `run_once_before_0x-install-homebrew.sh.tmpl` is the dominant convention (twpayne, liby, timriley, hilli, halostatue dotfiles). Nobody uses a bootstrap script to install CLT/Homebrew.
- Installer `--purge-binary` removes the curl-installed chezmoi after init; Homebrew's `brew "chezmoi"` takes over.
- Symlinked preferences break on macOS 14+ (cfprefsd rewrites the file), which kills stow/dotbot/mackup for `~/Library/Preferences`. mackup README says so; last release 0.11.2 (2024). https://github.com/lra/mackup
- nix-darwin: fully declarative but root activation (`system.primaryUser`), Determinate installer needs `nix.enable = false`, messy uninstall. Rejected. https://github.com/nix-darwin/nix-darwin
- Hammerspoon in chezmoi: dozens of repos vendor small configs as `dot_hammerspoon/`; only one has tests and it dropped busted/luarocks to fit. `git-repo` externals are "limited to running git clone and/or git pull", invisible to `chezmoi diff`, and a bare `git pull` in a dirty checkout aborts apply (twpayne/chezmoi#2495). Decision: separate repo, cloned by a `run_once_` script, installed by its own `make install`. Karabiner: community uses `run_onchange_` scripts hashing the inputs to regenerate `karabiner.json` (e.g. Gai-H/dotfiles).
- Karabiner docs: config path `~/.config/karabiner/karabiner.json`; a symlinked file breaks change detection; symlink the directory or set `XDG_CONFIG_HOME` instead. https://karabiner-elements.pqrs.org/docs/manual/misc/configuration-file-path/
- Fisher install: `curl -sL https://raw.githubusercontent.com/jorgebucaran/fisher/main/functions/fisher.fish | source && fisher install jorgebucaran/fisher`; commit `~/.config/fish/fish_plugins`; `fisher update` installs exactly that list. https://github.com/jorgebucaran/fisher
- fish default shell: `command -v fish | sudo tee -a /etc/shells` (guard with grep), `chsh -s "$(command -v fish)"`. Homebrew in fish: `/opt/homebrew/bin/brew shellenv fish | source`.
- gh: `gh auth status >/dev/null 2>&1 || gh auth login --git-protocol ssh --skip-ssh-key`; `gh ssh-key add ~/.ssh/id_rsa.pub --title "$(hostname)"`. https://cli.github.com/manual/gh_auth_login
- mise reads `.tool-versions` and asdf plugins; optional later swap. https://mise.jdx.dev/dev-tools/comparison-to-asdf.html
- Homebrew Intel is Tier 3 from Sept 2026; Apple Silicon only in scope.

### macOS defaults on 15/26

- Catalogues: macos-defaults.com (active, 2026) and nix-darwin `modules/system/defaults/` (active). mathiasbynens `.macos` unmaintained since Oct 2020, catalogue only. Diffing: `defaults read > before; change; defaults read > after; diff` (yannbertrand's `diff.sh`), `plistwatch` (https://github.com/catilac/plistwatch), `prefsniff`.
- Pointer speed: on 26.1 System Settings stopped writing `com.apple.mouse.scaling` (yannbertrand/macos-defaults#438), and this Mac confirms defaults (1) disagree with live HID (0.6875). Writing the keys plus `activateSettings -u` still applies them (verified 26.6.2). Whether the slider reflects the value and whether it survives reboot is unverified; test on the new Mac. `hidutil property --set '{"HIDMouseAcceleration": N}'` (N = speed × 65536) is live-only, not persisted, not shown in the pane. `HIDPointerResolution` is ignored by Apple trackpads/Magic Mouse. LinearMouse (config `~/.config/linearmouse/linearmouse.json`) cannot change speed on Apple trackpads. https://github.com/linearmouse/linearmouse/issues/1004
- `activateSettings -u` must run as the logged-in user in their session; some users saw settings revert during the day (nix-darwin#1475) and the GUI show stale state (nix-darwin#1207).
- Sandboxed app prefs (Safari, Messages) need Full Disk Access for the terminal; dropped. `com.apple.universalaccess` also needs FDA; dropped.
- Screenshot keys, Finder NewWindowTarget keys, Dock keys, WindowManager keys all confirmed present and honoured on 26.6.

## 6. Live-test note

During pointer research an agent wrote and applied test values for trackpad scaling, mouse scaling and double-click threshold on this Mac, then restored them. Verified afterwards: `com.apple.trackpad.scaling` = 1, mouse keys absent, live `HIDPointerAcceleration` = 45056, as before.

## 7. Open items

- Confirm True Tone and auto-brightness are off on this Mac (sudo read above) before implementing the display module.
- Sort the package dump into core and groups; propose, then review.
- Decide whether the self-patched Blex Nerd Font files are replaced by the `font-blex-mono-nerd-font` cask.
- Test on the new Mac after first bootstrap: pointer speed persists across reboot; dictation shortcut works after consent; Dock allow-list; wallpaper.
