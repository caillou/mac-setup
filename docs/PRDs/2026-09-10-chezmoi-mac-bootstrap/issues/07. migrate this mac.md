---
status: todo
needs-human: true
---

# Migrate this Mac

## PRD

[2026-09-10-chezmoi-mac-bootstrap](../prd.md)

Captured values, commands and sources are in [research-notes.md](../research-notes.md).

## What to build

Make the personal Mac the first machine kept in sync, reviewing every change before it lands.

- Adopt the current home files into the repo where the earlier issues left placeholders: fish files, git files, ssh config, zsh and bash files, iTerm2 preferences export.
- Replace the `~/.hammerspoon/keyboard` symlink and the old `init.lua` with the managed Lua files; confirm Ctrl+S layout works; delete `~/.hammerspoon/Spoons/EmmyLua.spoon` if unwanted.
- Archive `caillou/keyboard` and `caillou/karabiner.ts` on GitHub (`gh repo archive`) after adding a README line pointing at dotfiles; remove `~/repos/keyboard` and `~/repos/caillou/karabiner.ts` once the Karabiner build from the dotfiles copy has produced identical rules.
- Karabiner profile: already done on 2026-09-11. The only profile on this Mac was renamed to "Default profile" and the karabiner.ts source retargeted and committed; a rebuild produced identical rules. Nothing to do here beyond confirming the build from the dotfiles copy matches.
- Purge the dead fish universal variables listed in the research notes (BrowserStack, pyenv, Spacefish, colours, init markers); the settings kept move to the conf.d file created in the shell issue.
- Retire the git-clone asdf: remove its sourcing, keep `~/.asdf` as data dir, install Homebrew asdf, confirm node and python still resolve.
- Remove the old `fishfile` after `fish_plugins` is in place.
- Run the first `chezmoi apply` on this Mac with `chezmoi diff` reviewed by the user beforehand; expected diff includes the gitconfig change (BKW and Apprentice overrides gone, ICFM override added), the shell files, and the defaults script running once.
- Verify afterwards: prompt and abbreviations unchanged, git identity per folder, Downloads view intact, Dock unchanged, nothing else visibly different.

## User stories covered

2, 3, 24, 48

## Acceptance criteria

- [ ] `chezmoi diff` is empty after the first apply and `chezmoi status` is clean
- [ ] the universal variables file contains none of the dead entries
- [ ] `asdf` is the Homebrew binary and the installed node and python versions still work
- [ ] git commits under `~/repos/icfm` use the ICFM address
- [ ] both keyboard repos are archived on GitHub and their local checkouts removed; Hammerspoon and Karabiner behave as before
- [ ] the user has reviewed the diff before the apply

## Blocked by

- [01. rename repo and move local checkout.md](<./01. rename repo and move local checkout.md>)
- [06. readme.md](<./06. readme.md>)
