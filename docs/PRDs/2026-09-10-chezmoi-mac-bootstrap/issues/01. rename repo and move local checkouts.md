---
status: todo
needs-human: true
---

# Rename repo and move local checkouts

## PRD

[2026-09-10-chezmoi-mac-bootstrap](../prd.md)

Captured values, commands and sources are in [research-notes.md](../research-notes.md).

## What to build

Put the repo and the keyboard projects where the PRD says they live, on this Mac and on GitHub.

- Rename `caillou/mac-setup` to `caillou/dotfiles` on GitHub (`gh repo rename`); GitHub redirects the old URL. The repo stays public.
- Move the local checkout from `~/repos/mac-setup` to `~/repos/caillou/dotfiles` and point `origin` at the new URL.
- Move `~/repos/keyboard` to `~/repos/caillou/keyboard`, then re-run its `make install` so the `~/.hammerspoon/keyboard` symlink targets the new location and Hammerspoon reloads.
- `~/repos/caillou/karabiner.ts` already lives in the right place; verify its remote.
- Leave `~/repos/legacy/mac-setup` untouched.
- Confirm afterwards that `git status` is clean in all three repos and Hammerspoon's console shows the config loaded.

## User stories covered

48

## Acceptance criteria

- [ ] GitHub repo is `caillou/dotfiles`, public, and `git remote -v` in `~/repos/caillou/dotfiles` shows the new URL
- [ ] `~/repos/mac-setup` no longer exists
- [ ] `~/repos/caillou/keyboard` exists, `~/.hammerspoon/keyboard` points at it, and Hammerspoon runs the config after reload
- [ ] `~/repos/caillou/karabiner.ts` remote is `caillou/karabiner.ts`

## Blocked by

None - can start immediately
