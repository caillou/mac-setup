---
status: done
needs-human: false
---

# Finder column view and window manager keys

## PRD

[2026-09-10-chezmoi-mac-bootstrap](../prd.md)

Captured values, commands and sources are in [research-notes.md](../research-notes.md).

## What to build

Six keys the research notes mark as desired on all machines but the PRD's defaults list omitted, added to `run_onchange_after_60-defaults.sh.tmpl`.

- Finder block (next to the other `com.apple.finder` writes, around line 117): `FXPreferredViewStyle` `-string clmv`. Column view is the global default (research notes line 99); Downloads keeps its list view through the `.DS_Store` record script 62 writes, so the two do not fight.
- Window manager block (lines 147-150, after the four keys already there), the five keys the notes list as desired on line 105 and PRD line 143 does not carry. Types verified read-only on this Mac on 2026-09-11 with `defaults read com.apple.WindowManager` and `PlistBuddy -c Print`:
  - `StageManagerHideWidgets -bool false`
  - `AutoHide -bool false`
  - `EnableTiledWindowMargins -bool false`
  - `EnableTilingOptionAccelerator -bool false`
  - `AppWindowGroupingBehavior -int 1`
- The script stays a pure writer: no reads, no branches, no marker. It is `run_onchange_`, so the edit re-runs it on the next apply on both Macs.
- Tests: `tests/defaults.bats:371-374` ("what the notes mark informational or dropped is never written") currently pins the *absence* of `FXPreferredViewStyle`; drop it from that grep alternation and assert the write in the Finder values test instead. Extend the window manager test (`tests/defaults.bats:322-328`) with the five new lines.
- The PRD line 143 and the notes wording are corrected in [06. docs adopt the implemented refinements and migration precautions.md](<./06. docs adopt the implemented refinements and migration precautions.md>); this issue changes the script and the tests only.

## User stories covered

35, 36, 37

## Acceptance criteria

- [ ] the rendered script writes all six keys with the domains and types above, in the Finder and window-manager blocks
- [ ] the "informational or dropped" test no longer pins `FXPreferredViewStyle` and a test asserts the write
- [ ] the window manager test covers all nine `com.apple.WindowManager` keys
- [ ] the script is still a pure writer (no `defaults read`, no marker) and `tests/shellcheck-scripts.sh` is clean
- [ ] `bats tests` green

## Blocked by

None - can start immediately
