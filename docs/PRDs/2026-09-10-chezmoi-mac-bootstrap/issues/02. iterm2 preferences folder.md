---
status: todo
needs-human: false
---

# iTerm2 preferences folder

## PRD

[2026-09-10-chezmoi-mac-bootstrap](../prd.md)

Captured values, commands and sources are in [research-notes.md](../research-notes.md).

## What to build

Keep iTerm2's profile, font and colours in the repo, syncing both ways.

- A dot-prefixed folder `.iterm2/` in the chezmoi source directory (ignored as a target by chezmoi, so iTerm2 can write into the repo directly); migration (later issue) exports the current `com.googlecode.iterm2.plist` into it. The old `ayu dark.itermcolors` moves there as reference.
- `run_after_63-iterm2.sh.tmpl`, every apply: exit unless iTerm2 is installed (it may arrive later via Self Service); then, comparing before writing, set `PrefsCustomFolder` to `{{ .chezmoi.sourceDir }}/.iterm2`, `LoadPrefsFromCustomFolder` true, `NoSyncNeverRemindPrefsChangesLostForFile` true and `NoSyncNeverRemindPrefsChangesLostForFile_selection` 2 (both are needed for "save automatically"). The custom-folder and NoSync keys are excluded from iTerm2's exported plist, so no machine path leaks into the repo.
- Document that iTerm2 writes back on quit, so changes made in iTerm2 appear as repo drift to commit.

## User stories covered

43

## Acceptance criteria

- [ ] on a fresh machine with iTerm2 installed, the first launch shows the Plex Mono profile and Ayu Dark colours
- [ ] changing a setting in iTerm2 and quitting updates the file in the repo
- [ ] the script is a no-op when the keys already point at the folder

## Blocked by

- [01. repo skeleton, machine facts and ci.md](<./01. repo skeleton, machine facts and ci.md>)
