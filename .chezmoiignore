# Repo-only content that must never become a file in the home directory.
#
# Dot-prefixed source entries (.github, .editorconfig, the .iterm2 preferences
# folder, and the .karabiner and .downloads-view project folders added by later
# issues) are ignored by chezmoi already, so only the rest needs a rule here.

README.md
lefthook.yml

docs
docs/**

tests
tests/**

# Kept until the README issue replaces it; not a dotfile either way.
macos
